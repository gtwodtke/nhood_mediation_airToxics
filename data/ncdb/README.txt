Data from the Geolytics Neighborhood Change Database (NCDB) version 2.0 are proprietary and cannot be distributed directly by the authors.

The Requests folder contains "request" files for quickly replicating data extracts via the NCDB interface application.

The Codebooks folder contains metadata for the files assembled via the "requests."